# Server
PORT=3001
NODE_ENV=development

# Contact form (Resend). Leave blank to run in stub mode (logs instead of sending).
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=onboarding@resend.dev
